const Pool = require('pg').Pool
const pool = new Pool({
    user: 'learn_user',
    password: 'learn_user_57',
    host: 'localhost',
    port: 5432,
    database: 'learn_db'
})

module.exports = pool